<controls:MetroWindow x:Class="Health.Net.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="Health.Net" 
                      BorderBrush="{DynamicResource AccentColorBrush}"
                      BorderThickness="1"
                      SizeToContent="WidthAndHeight"
                      ResizeMode="NoResize"
                      MinHeight="350" 
                      MinWidth="525">
  <TabControl x:Name="tabs" BorderThickness="0 1 0 0" BorderBrush="{DynamicResource AccentColorBrush}" MinWidth="525">
    <TabItem Header="Food Log" x:Name="foodLogTabItem" />
    <TabItem Header="Create Food" x:Name="createFoodTabItem" />
  </TabControl>
</controls:MetroWindow>
